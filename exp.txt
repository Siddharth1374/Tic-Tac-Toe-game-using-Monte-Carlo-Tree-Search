"""
    Represents one node in the MCTS tree.
    Each node stores:
      - state       : the board at this point
      - player      : whose turn it is NEXT (the player who will move from here)
      - move        : the move that led to this state (None for root)
      - parent      : pointer to parent node
      - children    : list of explored child nodes
      - wins        : total wins accumulated through this node
      - visits      : how many times this node was visited
      - untried     : moves not yet expanded into child nodes
    """
    UCB1 = (wins / visits) + C * sqrt(ln(parent_visits) / visits)
                    ───────────────   ─────────────────────────────────────
                    exploitation term         exploration term

        - High visits  → we know more, less exploration bonus
        - Low  visits  → we know less, higher exploration bonus
        - C (≈√2)      → tunable constant balancing the two terms

        """
    PHASE 1 — SELECTION
    Starting from the root, keep choosing the best child (via UCB1)
    until we reach a node that is not fully expanded OR is terminal.
    """
    """
    PHASE 2 — EXPANSION
    Pick one untried move, apply it, and create a new child node.
    Returns the new child (or node itself if terminal).
    """
    %LINE:128
     # the PREVIOUS player made this move
    # Correction: node.player is who moves NEXT, so the move is made by other_player(node.player)
    # Actually let's clarify: node.player = who moves at this node

    """
    PHASE 3 — SIMULATION (Rollout)
    From the new node, play a completely RANDOM game until it ends.
    Returns the winner ('X', 'O') or None (draw).
    """
     """
    PHASE 4 — BACKPROPAGATION
    Walk from the leaf back up to root, updating win/visit counts.
    We increment wins when the result favours the AI player.
    """
    """
    Run MCTS for a given number of iterations and return the best move.

    Args:
        board       : current board state (list of 9: None/'X'/'O')
        ai_player   : which player the AI is ('X' or 'O')
        iterations  : how many simulations to run (more = stronger)

    Returns:
        best_move   : index (0-8) of the chosen cell
    """